{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin{% endblock %}

{% block body %}
    <h1>⚙️ Administration</h1>
    <p>Bienvenue {{ app.user.firstName }}, vous gérez l'application.</p>

    <div class="dashboard-cards">
        <div class="card">
            <h2>🏠 Salles</h2>
            <a href="{{ path('app_room_index') }}" class="btn">Gérer les salles</a>
            <a href="{{ path('app_room_new') }}" class="btn btn-primary">+ Nouvelle salle</a>
        </div>

        <div class="card">
            <h2>👥 Utilisateurs</h2>
            <a href="{{ path('app_user_index') }}" class="btn">Gérer les utilisateurs</a>
        </div>

        <div class="card">
            <h2>📅 Réservations</h2>
            <a href="{{ path('app_reservation_index') }}" class="btn">Toutes les réservations</a>
        </div>
    </div>
{% endblock %}