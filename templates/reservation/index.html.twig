{% extends 'base.html.twig' %}

{% block title %}Reservation index{% endblock %}

{% block body %}
    <h1>Reservation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>StartDatetime</th>
                <th>EndDatetime</th>
                <th>CreatedAt</th>
                <th>CancelledAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.id }}</td>
                <td>{{ reservation.startDatetime ? reservation.startDatetime|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ reservation.endDatetime ? reservation.endDatetime|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ reservation.createdAt ? reservation.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ reservation.cancelledAt ? reservation.cancelledAt|date('Y-m-d H:i:s') : '' }}</td>
                <td style="display:flex; gap:0.5rem;">
                    <a href="{{ path('app_reservation_show', {'id': reservation.id}) }}" class="btn">👁 Voir</a>
                    <a href="{{ path('app_reservation_edit', {'id': reservation.id}) }}" class="btn">✏️ Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservation_new') }}">Create new</a>
{% endblock %}
